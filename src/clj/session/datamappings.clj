(ns session.datamappings
  (:require
    [session.datatypes :as dt]))


(def data-mappings
  [{:record session.datatypes.Loop :tag "session/Loop"}
   {:record session.datatypes.Session :tag "session/Session"}])




